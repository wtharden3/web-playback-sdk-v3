const Login = () => {
  return(
    <div className="App">
      <header className="App-header">
        <a className="btn-spotify" href="/auth/login" >Login to Spotify</a>
      </header>
      {console.log(`login page ends`)}
    </div>
  )
}

export default Login;